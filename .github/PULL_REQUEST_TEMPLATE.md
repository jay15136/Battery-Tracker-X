## What does this change?

<!-- A concise description of what changed and why. Link the issue this
     addresses, if any (e.g. "Closes #12"). -->

## How was this tested?

<!-- New/changed repository or domain logic needs a test against a real
     SQLite database, not a mock. New UI needs at least an empty-state and
     a populated-state widget test. If this is a pure doc/config change,
     say so. -->

## Checklist

- [ ] `dart format --output=none --set-exit-if-changed .` passes
- [ ] `flutter analyze` passes with no issues
- [ ] `flutter test` passes (including new tests for this change)
- [ ] `flutter build windows` succeeds
- [ ] Updated `docs/IMPLEMENTATION_PLAN.md` and/or the relevant feature
      `README.md` if this changes behavior
- [ ] No hard-coded demo data, placeholder functions, or mocked persistence
      introduced

## Screenshots (if UI changed)

<!-- Light and dark mode, if practical. -->
